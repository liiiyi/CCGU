# Reproduction environment used by the DGL pipeline.
# Python 3.8 is recommended to match the paper implementation.
#
# NOTE: this list records the versions the paper's environment was described with.
# It is NOT the environment this repository's reproduction was verified on: the
# CUDA wheels for dgl 2.1.0 are no longer published, so the verified GPU
# environment pins torch==2.1.2+cu121 and dgl==2.2.1+cu121.  For that exact
# environment, including the extra shared libraries DGL >= 2.2 needs, use:
#     bash reproduction/scripts/setup_env.sh
# which installs reproduction/requirements-lock.txt.
#
# tensorflow was removed from this list: it was imported by exp/exp_train.py for
# three tensor ops that are now plain numpy, and tensorflow==2.13.1 requires
# numpy<=1.24.3, so it could never be installed alongside the numpy pin below.
numpy==1.24.4
scipy==1.10.1
scikit-learn==1.3.2
networkx==3.1
matplotlib==3.7.5
tqdm==4.66.5
torch==2.1.2
torchdata==0.7.1
dgl==2.1.0
torch-geometric==2.4.0

# Optional community-detection backends:
#   infomap   --partition infomap
#   hnswlib   --partition test (CCD) and --partition nikm
# infomap==2.7.1
# hnswlib==0.8.0
